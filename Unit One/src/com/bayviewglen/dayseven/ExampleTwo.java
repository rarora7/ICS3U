package com.bayviewglen.dayseven;

public class ExampleTwo {

	public static void main(String[] args) {
		
		int secondsperminuite = 60;
		
		int minuitesperhour = 60;
		
		int hoursperday = 24;
		
		int dayperyear = 365;
				
				int secondsperyear = secondsperminuite * minuitesperhour * hoursperday * dayperyear;
				
				System.out.println(" The number of seconds in a year is (wait for it..):" + secondsperyear);
				
				
			final int SECONDS_PER_MINUTE = 60;	
			final int MINUTES_PER_HOUR = 60;
			final int HOURS_PER_DAY = 24;
			final int DAYS_PER_YEAR = 365;
			
		 final int SECONDS_PER_YEAR = SECONDS_PER_MINUTE * SECONDS_PER_MINUTE * HOURS_PER_DAY * DAYS_PER_YEAR;
	
		 System.out.println(" The number of seconds in a year is (wait for it..):" + SECONDS_PER_YEAR);
		 
		 
		 double raidus = 4.2;
		 double area = Math.PI * Math.pow(raidus, 2);
		 
		 Math.random(1..
		 
		 // Math.random() +1 to make 1 your minimum number not 0 or 10* (Math.random() + 1
				 // [-10...10] there are 21 numbers so 21 * Math.random() -10) then cast as an int for no decimals 
				 
				 // 
	
	
	
	}

}
